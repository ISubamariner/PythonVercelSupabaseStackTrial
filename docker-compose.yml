services:  # No 'version: 3.8' line above this!
  backend:
    # ... (existing backend service definition) ...
    build: .
    ports:
      - "5000:5000"
    env_file:
      - .env
    volumes:
      - .:/usr/src/app
  
  # --- New Service for Running Tests ---
  tests:
    build: .
    env_file:
      - .env
    command: python -m pytest
    volumes:
      - .:/usr/src/app